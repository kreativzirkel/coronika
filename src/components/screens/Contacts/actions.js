export const addContact = (contact) => ({ type: 'ADD_CONTACT_CONTACTS', contact });

export const addLocation = (location) => ({ type: 'ADD_LOCATION_CONTACTS', location });

export const setActiveTab = (activeTab) => ({ type: 'SET_ACTIVE_TAB_CONTACTS', activeTab });

export const setSearchValue = (searchValue) => ({ type: 'SET_SEARCH_VALUE_CONTACTS', searchValue });
